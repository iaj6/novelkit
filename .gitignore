node_modules/
.env
.env.local
.env.production
*.log
dist/

# OS / editor cruft
.DS_Store
*.swp
*.swo
*~
.~lock.*
Thumbs.db

# JetBrains IDE config — user-specific, shouldn't be in the repo
.idea/

# Press build artifacts (regenerable from library/<book>/draft/ + canon/)
# - /build/ — global outputs (fontconfig cache, voices.tsv)
# - library/*/manuscript.md — produced by press/concat_chapters.sh
# - library/*/build/ — per-book HTML/EPUB/PDF/cover/audio
/build/
library/*/manuscript.md
library/*/build/

# Test coverage artifacts (generated; not source)
coverage/
.coverage
.coverage.*
htmlcov/
__pycache__/
*.pyc
.pytest_cache/
